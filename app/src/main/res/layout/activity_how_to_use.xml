<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_how_to_use"
    tools:context=".HowToUseActivity">

    <ImageView
        android:id="@+id/btnBackHowToUse"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:padding="@dimen/padding"
        android:layout_margin="@dimen/padding"
        app:srcCompat="@drawable/ic_back" />

    <ImageView
        android:id="@+id/btnExit"
        android:layout_width="60dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="@+id/btnBackHowToUse"
        app:layout_constraintBottom_toBottomOf="@+id/btnBackHowToUse"
        app:layout_constraintEnd_toEndOf="parent"
        android:paddingEnd="@dimen/padding"
        android:layout_margin="@dimen/padding"
        android:rotation="45"
        app:srcCompat="@drawable/ic_baseline_add_circle_24"
        app:tint="@color/blue" />
</androidx.constraintlayout.widget.ConstraintLayout>